doctype html
html
	head
		title Natours | #{tour}
		link(rel="stylesheet", href="css/style.css")
		link(rel="shortcut icon", href="img/favicon.png", type="image/png")
	
	body
		//-using local (buffered code)
		h1= tour
		//-Can write js in buffered code
		h2= user.toUpperCase()
		//- Unbuffered Code i.e which does not output to the page ...starts with -
		- const x = 9
		h2= 2 * x
		p This is just some text

